{% extends 'base.html.twig' %}

{% block title %}Connexion!{% endblock %}

{% block body %}

    {#<div class="content">#}
    {#{% if is_granted('IS_AUTHENTICATED_FULLY') %}#}
    {#<p>Welcome {{ app.user.firstname }}</p>#}
    {#<p>Votre mail: {{ app.user.email }}</p>#}
    {#{% else %}#}
    {#<p>Welcome Anonymous</p>#}
    {#{% endif %}#}

        {#<h1>Vote des Conference en ligne </h1>#}

        {#<h2>Voter pour votre conference preferer(soyer connecter)</h2>#}

        {#{{ path('user',{'id':user.id}) }}#}
        {#{% for conference in conferences %}#}
            {#{{ include('conference/details.html.twig') }}#}
        {#{% endfor %}#}

    {#</div>#}

    <!-- Hero section -->
    <section class="hero-section set-bg" data-setbg="img/bg.jpg">
        <div class="container h-100">
            <div class="hero-content text-white">
                {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                    <p>Welcome {{ app.user.firstname }}</p>
                    <p>Votre mail: {{ app.user.email }}</p>
                {% else %}
                    <p>Welcome Anonymous</p>
                {% endif %}
                <div class="row">
                    <div class="col-lg-6 pr-0">
                        <h2>Vote Conference</h2>
                        <p>connecter pour voter</p>
                        <a href="{{ path('list_conferences') }}" class="site-btn">GO GO GO</a>
                    </div>
                </div>
                <div class="hero-rocket">
                    <img src="./img/rocket.png" alt="">
                </div>
            </div>
        </div>
    </section>
    <!-- Hero section end -->
{% endblock %}