{% extends 'base.html.twig' %}

{% block title %}ADD USER !{% endblock %}

{% block body %}

    <div class="content">
        <ul>
            {{ form_start(form) }}
            {{ form_widget(form) }}
            {{ form_end(form) }}
        </ul>
    </div>
{% endblock %}